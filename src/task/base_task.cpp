// =====================================================================
// 基础任务类实现
// =====================================================================
//
// 审查注记：本 .cpp 仅实现了 BaseTask 的构造/析构两个平凡函数；
// 头文件中声明的 init()/run() 在这里**没有**定义——这是刻意的现状
// （占位基类），任何调用都会以"链接期未定义符号"暴露，而不是运行期
// 才崩溃。实际任务（PpeTask/HelmetTask）不依赖本实现文件。
//
#include "base_task.h"          // 基础任务类头文件

// =====================================================================
// 构造函数
// =====================================================================
// 作用：创建基础任务实例；空实现——m_stream_id/m_type 均未初始化
// （成员为预留字段，无读写点）。
// =====================================================================
BaseTask::BaseTask(/* args */)
{
}

// =====================================================================
// 析构函数
// =====================================================================
// 作用：空实现。注意：本析构**不是** virtual（见 base_task.h 现状注记），
// 因此严禁通过 BaseTask* 指针 delete 派生对象，否则派生类析构不会执行。
// =====================================================================
BaseTask::~BaseTask()
{
}

